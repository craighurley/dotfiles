# git
#export GIT_AUTHOR_NAME="First Last"
#export GIT_COMMITTER_NAME="$GIT_AUTHOR_NAME"
#export EMAIL="firstlast@example.com"

# homebrew
#export HOMEBREW_GITHUB_API_TOKEN=

# custom aliases
#alias sceptre="docker run --rm -it -v $HOME/.aws:/root/.aws:ro -v $(pwd):/workdir:ro sceptre"
