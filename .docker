#!/usr/bin/env bash

# Start
echo "$(basename "$0") starting."

# personal containers
docker pull craighurley/docker-weechat
docker pull craighurley/docker-znc

# public containers
docker pull flomotlik/awsinfo

# Finished
echo "$(basename "$0") complete."
