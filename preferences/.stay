#!/usr/bin/env bash

###############################################################################
# stay
###############################################################################

p=~/Library/Application\ Support/Stay/Screenshots/

# check if screen shots dir exists
[[ ! -d "$p" ]] && mkdir -p "$p"
[[ ! -d "$p" ]] && chmod -w "$p"
